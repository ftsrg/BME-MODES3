package hu.bme.mit.inf.modes3.messaging.messages.core

/**
 * Common ancestor for every message that is used by the Modes API.
 * These messages should be serialization / deserialization independent in order to keep the them separated from the specific technology
 */
abstract class InternalMessage {
	// class is empty on purpose
}
