apply plugin: 'java'
apply plugin: 'org.xtext.xtend'
apply plugin: 'com.github.johnrengelman.shadow'

dependencies {
	compile project(':utils:common')
	compile project(':utils:conf')
	compile project(':messaging:mms')
	compile project(':transports:mqtt')
}

def archivePrefix = "${messagingPrefix}.${project.name}"

jar {
	baseName archivePrefix
}

shadowJar {
	baseName archivePrefix
}